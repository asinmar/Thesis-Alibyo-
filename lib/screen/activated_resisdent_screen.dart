import 'package:flutter/material.dart';

class ActivatedResidentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Activated Resident Screeen'),
      ),
    );
  }
}
