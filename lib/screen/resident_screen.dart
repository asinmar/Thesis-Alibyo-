import 'package:flutter/material.dart';

class ResidentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Resident Screeen'),
      ),
    );
  }
}
